import SimpleFileUpload, { SimpleFileUploadProvider } from './'

describe('SimpleFileUpload', () => {
  it('is truthy', () => {
    expect(SimpleFileUpload).toBeTruthy()
  })
})

describe('SimpleFileUploadProvider', () => {
  it('is truthy', () => {
    expect(SimpleFileUploadProvider).toBeTruthy()
  })
})
